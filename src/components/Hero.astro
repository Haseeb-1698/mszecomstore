---
import { Search, ArrowRight } from 'lucide-react';
---

<section class="relative bg-cream-100 border-b border-cream-400 overflow-hidden">
  <div class="absolute inset-0 bg-grid-charcoal-900/[0.05] [mask-image:linear-gradient(to_bottom,white_5%,transparent_80%)]"></div>
  
  <div class="max-w-7xl mx-auto px-6 py-24 sm:py-32 lg:py-40 relative z-10">
    <div class="max-w-3xl text-center mx-auto">
      <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-charcoal-900 tracking-tighter !leading-tight">
        Premium Subscriptions,
        <span class="bg-clip-text text-transparent bg-gradient-to-r from-coral-500 to-purple-500">Simplified.</span>
      </h1>
      
      <p class="mt-6 text-lg sm:text-xl text-charcoal-800/80 max-w-2xl mx-auto">
        SubHub is your one-stop-shop for accessing top-tier services like Netflix, Spotify, and more, all at a fraction of the cost.
      </p>
      
      <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
        <div class="relative w-full max-w-md">
          <input 
            type="text" 
            placeholder="Search for services like Netflix, Spotify..." 
            class="w-full px-6 py-4 rounded-full border border-cream-400 bg-cream-50 focus:ring-2 focus:ring-coral-500 transition-all"
          />
          <div class="absolute right-4 top-1/2 -translate-y-1/2">
            <Search className="w-5 h-5 text-charcoal-800" />
          </div>
        </div>
        <button class="px-8 py-4 rounded-full bg-coral-500 text-white font-medium hover:bg-coral-600 transition-all flex items-center gap-2">
          <span>Explore</span>
          <ArrowRight className="w-5 h-5" />
        </button>
      </div>
    </div>
  </div>
</section>